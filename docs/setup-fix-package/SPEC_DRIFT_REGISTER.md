# Spec Drift Register
## Setup Fix Package · 9-PR run

Per `SETUP_FIX_PACKAGE_2026-05-07.md` §1.5. One entry per drift: where the spec was wrong, ambiguous, or required deviation.

| Entry | PR | Spec section | What spec said | What we did | Why |
|---|---|---|---|---|---|
| 1 | PR 1 | §3.2 — "do NOT add explicit redirects" | After removal, `/admin/architecture` should 404. | Also deleted `src/app/(maestro)/platform/admin/architecture/page.tsx` which `redirect()`-ed to `/admin/architecture`. | Leaving a redirect to a 404 surfaces a confusing two-hop error. The platform redirect was an existing legacy `redirect('/admin/architecture')` that no longer points anywhere meaningful; deleting it makes `/platform/admin/architecture` 404 directly, consistent with §3.2's intent. Logged because the spec scope is technically the four `/admin/*` panels only. |
| 2 | PR 1 | §6 — "MUST NOT modify any other surface" | Source/Strategic Moves/Tower/Intelligence/Learn out of scope. | Left `src/components/source/PatternRecommendationChips.tsx` link `/admin/reasoning/patterns` intact (broken). | Modifying Source surface is explicitly forbidden by §6. Following the spec produces link rot in Source — flagged for a separate PR (likely PR 9 polish or a follow-up). |
| 3 | PR 1 | §6 — "MUST NOT modify any other surface" | Docs surface is out of scope. | Left ~10 references to `/admin/reasoning` in `src/app/(maestro)/docs/reasoning/**` intact (broken). | Same as entry 2. Per §12.3 "stale doc references are acceptable spec drift." Docs CTAs to `/admin/reasoning` will 404 until docs are updated in a follow-up. |
| 4 | PR 1 | §6 — implicit | Global nav untouched; what about CommandPalette / GlobalSearchModal? | Removed entries pointing to deleted routes (1 in CommandPalette, 6 in GlobalSearchModal) and re-pointed pattern search results to `/intelligence/ask`. | These are global search features, not "global nav". Leaving them would surface 404s on every Cmd+K search; cleaning up was the right call. Pattern search re-target to `/intelligence/ask` chosen because Sentinel/Intelligence is the canonical pattern surface today (per memory `feedback_workflow_first_agents_hidden`). |
| 5 | PR 1 | §6 — implicit | Setup peer-page sub-nav arrays. | Removed the `architecture` entry from the SUB_NAV arrays in 8 Setup peer pages (`SetupAuditPage`, `SetupConnectorsPage`, `SetupPoliciesPage`, `ConnectorDetailPage`, `SetupUsersPage`, `SetupTenantPage`, `InviteCollaboratorPage`, `ConnectorReconnectPage`). | Each Setup page renders its own sub-nav with hardcoded entries. Leaving them would surface a "Architecture" tab pointing to a 404 on every Setup peer page. Strict reading of §6 only covers the four primary panels, but Setup peer-page sub-navs are clearly Setup territory. |
| 6 | PR 1 | §3.4 — preserve API endpoints when uncertain | API routes potentially shared. | Preserved `src/app/api/reasoning/**` (28 routes), `src/app/api/setup/initiatives*`, `src/lib/reasoning/**`, `src/lib/architecture/**`, `src/lib/build-progress/**`, `src/components/reasoning/**`, `src/lib/admin/architecture-page-view.ts`, `src/lib/admin/build-progress-page-view.ts`, and adapters. | Per §6 "MUST NOT remove API endpoints whose consumers you haven't verified" and §12.2 "preserve API endpoints if uncertain." Tower/Source/Programs surfaces import `src/lib/reasoning/**` and `src/components/reasoning/**` extensively. Logged as substrate gap (see SUBSTRATE_GAP_REGISTER entry 1) for follow-up audit. |
| 7 | PR 1 | §11 acceptance — "Reasoning panel inaccessible from Setup left-nav and via direct URL (including all 157 sub-routes)" | 157 sub-routes per spec. | 156 sub-routes deleted (matches the 156 we counted via `find src/app/(maestro)/admin/reasoning -type d`; the spec's "157" included the parent directory itself). | Off-by-one in spec's figure. Counted 156 sub-route directories below `reasoning/` plus 1 parent. Total deletion: parent + all sub-routes. |
| 8 | PR 1 | §3.5 — substrate not deleted | QA audit metadata in `src/lib/qa/wireframe-compliance-audit.ts`, `active-route-ownership-map.ts`, `route-shell-design-verification.ts` | Updated metadata to drop `/admin/architecture` and `/platform/admin/architecture` entries. Updated counts/expectations in QA tests. | These are integration-test data sources that assert the existence/score of routes. Architecture page being deleted is exactly the kind of state change these tests are designed to track. Updates were minimal: removed entries + adjusted counts. |
| 9 | PR 1 | §1.4 failure matrix — lint pre-existing | Pre-existing breakage logged, do NOT fix in this PR. | Did not fix 9 pre-existing lint errors in `src/components/source/VendorScorecardMatrix.tsx` (1 × `react-hooks/purity` Math.random in render) and `src/components/tower/TowerIndexPage.tsx` (8 × `react/no-unescaped-entities`). | Both files are on out-of-scope surfaces (Source, Tower). My files lint clean (0 errors, 1 pre-existing warning in `GlobalSearchModal`). Per spec §1.4, pre-existing lint failures unrelated to my changes are logged and not blocking. |
| 10 | PR 1 | §1.4 failure matrix — test pre-existing | Pre-existing breakage logged, do NOT fix in this PR. | Did not fix 2 pre-existing test failures: `wireframe-compliance-audit.test.ts > getPagesRequiringWave32 > all returned pages have at least one remaining deviation` and `Intelligence page held at 84 after Wave 32` (received 86, expected 84). | Verified by `git stash` + running on main — both fail on `main` independent of my changes. Per spec §1.4, log and proceed. |
| 11 | PR 1 | §1.4 failure matrix — test pre-existing | Pre-existing breakage logged, do NOT fix in this PR. | Did not fix the `admin-shell-v2.test.ts` source-content tests that assert: (a) AdminCanonShellV2 grid template `'280px 1fr 320px'` (file uses `'240px minmax(0, 1fr) minmax(360px, 28vw)'`), (b) AdminCanonShellV2 imports from `@/lib/design/design-tokens` (file imports from `@/lib/shell/shell-tokens`), (c) AdminSidebar uses `'use client'` single-quotes (file uses double-quotes), (d) AdminSidebar `aria-current` single-quotes (file uses double-quotes), (e) banned token literal checks. | All these tests assert lexical patterns in the source files. The patterns asserted don't match the actual files on `main`. Pre-existing source-vs-test divergence. |
| 12 | PR 2 | §3 fix strategy — convergent helper | Spec preferred a single helper change. | Created `src/lib/admin/admin-tenant.ts` with `resolveAdminTenant()` and wired it into the 4 broken pages + users-access. Also patched the only hardcoded `'apex-retail'` literal inside a builder (`data-trust-page-view.ts:545`). | Convergent helper minimizes per-page churn. Other 3 builders already passed `tenantSlug` to `buildAgentContextAsync` correctly; data-trust was the outlier. |
| 13 | PR 2 | §12.2 don't fix panel content | Tenant routing only. | Did NOT touch hardcoded "Apex Retail" engagement names / fixture strings inside `data-trust-page-view.ts` (lines 277-312, 427) and `production-readiness-page-view.ts` (line 191). | These are inline fixture strings that will continue showing "Apex Retail" in some columns even for FCF tenants. Per spec §12.2 ("Do NOT improve panel content as part of this PR"), out of scope for PR 2. PRs 6-9 will address content via redesigns. Flagged for follow-up. |
