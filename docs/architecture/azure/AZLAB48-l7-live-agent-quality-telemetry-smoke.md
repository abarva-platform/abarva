# AZLAB48 - L7 Live Agent Quality Telemetry Smoke

Status: executed in Azure lab  
Date: 2026-05-15  
Layer: L7 agent quality / L4 tenant isolation / L11 observability

## Why This Matters

AZLAB46 created the durable telemetry table and AZLAB47 added the smoke harness. This entry records the first live Azure execution: the migration was applied in Azure Postgres from the private Container Apps environment, then the smoke proved the table and RLS behavior in the lab database.

## Image

| Field | Value |
|---|---|
| ACR image | `acrabarvalab001.azurecr.io/abarva/web:lab-agent-quality-telemetry-20260515-r1` |
| Digest | `sha256:c5ea820f772efc747c2b91b2902cf38bd00f977be95250ed2d65316899ed1051` |

## Migration Execution

The database migration job was updated to the current image and started as:

`job-abarva-db-migrate-lab-eastus-au9h2wf`

Execution status: `Succeeded`

Log evidence:

```text
Pending migrations (2):
   - 20260515120000_tenant_key_canonicalization.sql
   - 20260515210000_agent_quality_violation_events.sql
→ 20260515120000_tenant_key_canonicalization.sql ... ✓
→ 20260515210000_agent_quality_violation_events.sql ... ✓
✓ 2 migrations applied.
```

## Telemetry Smoke Execution

The same private Container Apps job was temporarily repointed to:

```bash
npx tsx src/scripts/agent-quality-telemetry-smoke.ts \
  --tenant-key apex-retail \
  --other-tenant-key meridian-health \
  --commit-fixture
```

Execution:

`job-abarva-db-migrate-lab-eastus-benou64`

Execution status: `Succeeded`

Result:

```json
{
  "status": "pass",
  "event": "l7_agent_quality_telemetry_smoke",
  "tableExists": true,
  "tenantIndexExists": true,
  "ginIndexExists": true,
  "insertedId": "vlt_l7_smoke_1778882086239",
  "serviceVisibleRows": 1,
  "authenticatedTenantVisibleRows": 1,
  "authenticatedOtherTenantVisibleRows": 0,
  "committed": true
}
```

Interpretation: Azure Postgres now has the durable guard telemetry table, the dashboard can read persisted guard events, and RLS prevents a Meridian-authenticated context from reading the Apex smoke event.

## Cleanup

After the smoke, the Container Apps job command was restored to its normal migration command:

```bash
npx tsx src/scripts/bootstrap-azure-postgres-compat.ts &&
npx tsx src/scripts/run-migrations.ts --ci --allow-destructive
```

## Next Step

Run the L7 live answer-quality baseline against Azure/prod using the 50-case corpus and a short-lived authenticated cookie, then verify the C5 pilot dashboard shows persisted guard telemetry generated by real Sentinel/Atlas/Nexus/Source/Steward turns.
