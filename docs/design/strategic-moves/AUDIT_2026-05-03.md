# Strategic Moves — Visual audit vs design reference

**Date:** 2026-05-03  
**Reference:** `docs/design/strategic-moves/14-strategic-moves-home.html`, `tokens.css`, `README.md`, `INTEGRATION.md`  
**Live implementation:** `src/components/strategic-moves/*`, `StrategicMoves.module.css`

Severity: **high** = wrong fundamentals (color, type, layout vs canon); **medium** = proportions, spacing, missing element; **low** = polish.

---

## 1. Page shell & background

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `StrategicMoves.module.css` `.page` | `background: #f8f5ef` (hard-coded) | Authenticated cream `--canon-bg-cream` (`#f5f1eb` per `tokens.css`) | **high** |

---

## 2. Editorial ribbon (portfolio KPI strip)

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `StrategicMovesHomeClient.tsx` + `.editorialRibbon` / `.editorialMetric` | Four **card-like** tiles: stacked Fraunces **50px** value, mono label below | `.ribbon` + `.ribbon-seg`: **inline** serif num + mono label **on one baseline**, vertical hairline separators, fifth **meta** column (“Updated …”); interactive filter segments in prototype | **high** (layout + information architecture differ from binding HTML) |
| Same | No “Updated …” meta column | `.ribbon-meta` present in prototype | **medium** |
| Same | No filter interaction on ribbon segments | Prototype `ribbon-seg` drives status filter | **medium** (behavior; may be intentional MVP) |

---

## 3. KPI / metric typography

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.ribbonValue` | `font-size: 50px` (serif) | Prototype `.ribbon-seg .num` uses **36px** Fraunces **700** with underline affordance on hover/active | **medium** (size + weight; audit brief asked for **32px / 500** for KPI tile — prototype HTML is **36/700**; reconcile README vs HTML vs this audit line) |
| `.ribbonLabel` | Mono 10px, color `#716754` | Prototype `.lbl` uses mono **9.5px**, **1.6px** letter-spacing, **`--gold`** in prototype → **must map to `--abarva-signal-blue`** per `INTEGRATION.md` (live uses brown-gray, not signal blue) | **medium** |

---

## 4. Phase rail

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `StrategicMovesHomeClient.tsx` (cards) | Dot rail **present** at bottom of card | `.detail-rail` / card footer pattern in prototype | **low** (cards aligned) |
| `StrategicMoveDetailView.tsx` | Section title “Phase Rail” + dots in `.phaseRail` | Same dot-rail idiom as prototype `detail-rail` (no redundant section chrome in prototype — rail sits under status) | **medium** (structure/labeling vs prototype) |
| `StrategicMoveOriginateClient.tsx` | **No** phase rail in right pane | Prototype originate includes `#origin-rail` / `.detail-rail` for P0…P7 | **high** (missing shared `PhaseRail` on Originate) |
| Code organization | Inline `Array.from({ length: 8 })` in multiple files | Single **`PhaseRail.tsx`** per `.cursorrules` / `INTEGRATION.md` | **medium** (maintainability + consistency) |

---

## 5. Card grid (portfolio cards view)

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.cards` | `repeat(auto-fill, minmax(260px, 1fr))` (fluid columns) | This audit’s target: **3-up** grid; prototype HTML uses **`1fr 1fr`** (2-up) in `.cards` — **spec conflict**: align README / HTML / product to one column count | **high** (until spec is single source) |
| `.cardTitle` | Fraunces **16px**, default weight | Target **18px / 500** per audit brief; prototype `.card-title` is **17px / 700** | **medium** |
| Card chrome | `#f6f2e8` fill, 10px radius | Prototype `.card`: `--cream-card`, **14px** radius, **4px** left semantic strip (`.strip`) | **medium** |
| `.metaRow` | Status chip + **phase text** only | Prototype includes richer **card-meta** row (value line, etc.) | **low** (data-dependent) |

---

## 6. Status chip color discipline

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.cardTeal` / `.bubbleTeal` | Left border / bubble fill uses **`#0066cc`** (signal blue) | Semantic **teal** for validated / healthy early — should use **`--canon-teal`** / canon palette, not marketing signal blue | **high** (decorative misuse of brand blue for a “teal” status bucket) |
| `.chipTeal` | Uses `--canon-teal-dark` / `--canon-teal-light` | Consistent semantic teal | **low** |

---

## 7. “+ New Move” primary CTA

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.newMove` | Already **ink black** bg + cream text | `INTEGRATION.md`: black, not signal blue | **low** (aligned) |

---

## 8. Sort chips & view toggle

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.toggleButton` / `.toggleButtonActive` | Pill toggles, active = **#111** on white text | Prototype `view-toggle` / `sort-select` differ (select + tablist); not pixel-matched | **medium** |
| Labels | Raw lowercase `scatter`, `cards`, `value` | Prototype uses **Title Case** / sentence labels for controls | **low** |

---

## 9. Scatter / map block

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| Title block | Toolbar only; no “Portfolio map” **h2** block | Prototype `.map-title-block` + `.map-sub` | **medium** |
| Implementation | HTML/CSS scatter with divs | Prototype uses **SVG** `scatter` for placement | **low** (functional parity may be OK) |
| `.scatterLegend` / `.scatterYAxis` / `.scatterPhaseCode` | Colors **`#8b6830`** (gold-adjacent) | Canon neutrals or mono on cream per token discipline | **medium** |

---

## 10. Originate — scaffold & Nexus panel

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.scaffoldRow` | Dashed border empty states **aligned** | Prototype empty scaffold rows with mono labels | **low** |
| `.scaffoldLabel` (empty) | `--canon-gray-500` | Filled uses signal blue per INTEGRATION — **OK** | **low** |
| `.primaryAction` (Send, Promote, dialog) | **`background: var(--abarva-signal-blue)`** | In-app primary must be **ink / cream**, not signal blue (`INTEGRATION.md`, `.cursorrules`) | **high** |
| `.chatHead` structure (Originate) | Eyebrow + plain div “Nexus drafting mode” | Detail prototype: Fraunces **agent name** sizing hierarchy | **medium** |

---

## 11. Detail — Nexus dark panel mono / “gold”

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.chatHead .eyebrow` | `color: #69a9ff` (light blue) | INTEGRATION: replace prototype **`--gold`** eyebrows on dark with **`--abarva-signal-blue`** — live is **not** using that canon eyebrow color | **medium** (discipline: align to documented mapping) |
| `.bubbleNexus strong` | `#9dc5ff` | Acceptable emphasis; verify contrast vs token doc | **low** |

---

## 12. Detail — primary action link

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.primaryActionLink` | **Signal blue** filled button | In-app primary CTA should be **ink on cream**, not signal blue (`INTEGRATION.md`) | **high** |

---

## 13. Attention / “Need attention” drilldown

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| Panel structure | Bordered panel + list | Prototype `.attn-drilldown` rows with branch glyph `└─` and arrow | **medium** |

---

## 14. Hero title

| Location | Live | Reference | Severity |
|----------|------|-----------|----------|
| `.title` | `48px` Fraunces | Token `--type-display-1` uses `clamp(48px, 6vw, 88px)` — live uses fixed **48px** | **low** |

---

## Summary counts

- **High:** 8  
- **Medium:** 12  
- **Low:** 8  

**Note:** Prototype `14-strategic-moves-home.html` still contains **`--gold`** in CSS; implementation must **not** copy `--gold` — use **`--abarva-signal-blue`** per `INTEGRATION.md` where the prototype used gold for mono labels on dark / filled scaffold labels.
