# Ava Regression Report

This report is initialized with the code-level gates in this slice. Browser crawl evidence should be appended after signed-in proof.

Required browser checks:

- Home answer has no stale history.
- Home answer has no `Read`, `Evidence`, `Current-state read`, or raw debug labels.
- Direct answer appears before proof.
- Home decision questions hand off instead of recommending.
- Intelligence answers may use experts, but separate tenant facts from corpus/pattern context.
