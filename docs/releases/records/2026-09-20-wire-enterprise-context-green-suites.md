# 2026-09-20-enterprise-context-green-suite-ownership — Enterprise context CI ownership

## Release ID

`2026-09-20-enterprise-context-green-suite-ownership`

## Status

`candidate`

## Plain-English Summary

Seven measured-green enterprise-context suites now run for every pull request.
Two failing files remain named, exact-file quarantines instead of hiding the
whole directory.

## Layer Impact

- `global-control-lane`: test and release-control coverage only.
- No client intake, adapter, canonical-model, product-runtime, schema, or data
  changes.

## Client Applicability

- All clients: indirect regression-protection benefit only.
- Specific clients: none.
- Internal only: CI workflow and coverage evidence.
- Public/demo only: no.
- Feature flag: none.

## Changes Included

- Add the seven measured-green suite files to the pull-request unit workflow.
- Add a behavioral contract for the explicit green set and generated census.
- Refresh the generated test-coverage census.

## QA / Validation

- PASS: measured nine files as nine loaded, nine collected, nine run, seven
  green, and two red before editing.
- PASS: counted non-test importers for every production module under test.
- PASS: ran the seven-file workflow command locally.
- PASS: ran the behavioral contract locally.
- PASS: mutation check proved removing a green workflow entry fails both the
  explicit-file assertion and the census split.
- PASS: generated census check, TypeScript, focused ESLint, and release check.

## Rollout Plan

Squash-merge through the protected repository. The repository-owned main ACA
workflow carries the exact merge even though the change affects CI only.

## Deployment Authority

- Repo-owned deploy workflow: `.github/workflows/aca-main-deploy.yml`.
- Shared runtime mutators: repository workflow only.
- Approved image digest: recorded from the post-merge workflow artifact.
- ACA runtime invariant: template, active revision, and worker digests must
  match before deployed status is recorded.
- Worker image invariant: required.
- Feature/env flag update path: not applicable.
- Live signed-in proof required: no; this is a CI-only control change.

## Rollback Plan

Revert the workflow, behavioral test, census refresh, and this record. The two
red suites remain visible product work and are not altered by rollback.

## Audit Evidence

- Workflow names all seven green files explicitly.
- Behavioral test requires seven of nine files covered in the flat directory.
- Committed census is regenerated by the repository-owned census tool.
- Pull-request checks and the post-merge deploy artifact complete the record.

## Known Gaps

- `derived-enterprise-read.test.ts` remains red because retired local inputs
  are absent from a live read path.
- `intelligence-read-model.test.ts` remains red on two aggregation-shape
  expectations.
