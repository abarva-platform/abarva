# 2026-06-01-add-hooks — Add Commit Hooks

## Release ID

`2026-06-01-add-hooks`

## Status

`candidate`

## Plain-English Summary

This change adds local commit guardrails for staged formatting/linting and Conventional Commit messages. It installs Husky, lint-staged, and commitlint, documents the developer workflow, and records how to bypass hooks only in emergencies.

## Layer Impact

Ops-release-lane developer tooling only. The change affects local commit behavior and repository package metadata; it does not change runtime behavior, product UI, authentication, data access, migrations, or infrastructure.

## Client Applicability

- All clients: None.
- Specific clients: None.
- Internal only: AbarVa maintainers and contributors committing to this repository.
- Public/demo only: None.
- Feature flag: None.

## Changes Included

- `.husky/pre-commit`
- `.husky/commit-msg`
- `commitlint.config.js`
- `docs/runbooks/dev-environment.md`
- `docs/releases/records/2026-06-01-add-hooks.md`
- `package.json`
- `package-lock.json`

## QA / Validation

- Pass: Node.js `v24.15.0` used for package installation and validation.
- Pass: bad commit message rejected by commitlint.
- Pass: good commit message accepted by commitlint.
- Pass: `npx lint-staged --allow-empty`
- Pass: `git diff --check`
- Pass: `npm run release:check -- --base origin/main --head HEAD`

## Rollout Plan

Merge to `main`. Developers receive the hooks after pulling and running `npm install`; the `prepare` script installs Husky locally.

## Rollback Plan

Revert the commit to remove Husky hooks, lint-staged configuration, commitlint configuration, dependency additions, and the runbook.

## Audit Evidence

- Pull request for `codex/add-hooks`
- Local validation commands listed above
- CI checks on the pull request

## Known Gaps

Existing npm audit findings remain outside this tooling slice; this change does not attempt dependency remediation beyond installing the requested hook packages.
