# 2026-08-22-moves-adaptive-depth-proof-harness — Moves Adaptive Depth Proof Harness

## Release ID

`2026-08-22-moves-adaptive-depth-proof-harness`

## Status

`candidate`

## Plain-English Summary

Adds a repeatable internal audit command that proves Moves adaptive artifact depth behaves as
intended for straightforward and complex use cases. The command generates a timestamped proof
package with applicability decisions, story-beat decisions, final-format checks, and focused test
logs.

## Layer Impact

- Release lane: `internal-admin`.
- Layer 4 Products: Adds an internal Moves audit harness only. No product route, UI behavior,
  tenant data, canonical model, or projection data is changed.

## Client Applicability

- All clients: No runtime behavior change.
- Specific clients: None.
- Internal only: Audit/proof command for Moves artifact-depth validation.
- Public/demo only: None.
- Feature flag: None.

## Changes Included

- `scripts/audit/build-moves-adaptive-depth-proof.ts`
- `package.json` script `audit:moves-adaptive-depth-proof`

## QA / Validation

Status: `passed`.

- Passed: `npm run audit:moves-adaptive-depth-proof`
- Passed: Focused Jest suites emitted by the proof harness:
  - adaptive-depth resolver and generate-phase route tests
  - deliverable queue worker reconstruction tests
  - generated artifact download route tests
  - native DOCX/XLSX/PPTX renderer tests
- Passed: `npx eslint scripts/audit/build-moves-adaptive-depth-proof.ts src/lib/deliverables/adaptive-depth.ts src/lib/deliverables/__tests__/adaptive-depth.test.ts`
- Passed: `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit --pretty false --incremental false`
- Passed: `npm run release:check`

## Rollout Plan

Merge to main. The repo-owned deploy workflow may rebuild the shared application image, but this
change is an internal audit script and does not require a runtime feature rollout.

## Deployment Authority

- Repo-owned deploy workflow: Allowed for merge-to-main if triggered.
- Shared runtime mutators: None.
- Approved image digest: Not applicable before deploy.
- ACA runtime invariant: Standard deploy workflow invariant if merge triggers a deploy.
- Worker image invariant: Standard deploy workflow invariant if merge triggers a deploy.
- Feature/env flag update path: None.
- Live signed-in proof required: No; this is an internal report-only audit harness.

## Rollback Plan

Revert the PR. No data rollback is required because the command is report-only and writes only to
the operator-selected output directory.

## Audit Evidence

- Proof package generated by `npm run audit:moves-adaptive-depth-proof`
- PR diff and CI logs
- Release control gate output

## Known Gaps

The proof harness does not create a live Move, call Claude, or mutate generated artifacts. Live
signed-in generation remains a separate runtime proof.
