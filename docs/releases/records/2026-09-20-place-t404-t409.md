# 2026-09-20-place-t404-t409 — Six items the queue could not offer

## Release ID

`2026-09-20-place-t404-t409`

## Status

`candidate`

## Plain-English Summary

Six newly filed backlog items were not in the structure map, so the execution queue could
not offer any of them. The gate added earlier today blocked the board run until they were
placed, which is what it exists for — this is the second time it has caught a batch on the
first run after they were filed.

They are real work, and four of the six are the same shape as items already closed today:

- two concern merges that sit at a merge SHA without a deploy verdict, one of them a merge
  made from this session;
- four concern `critical`-band directories whose suites are reached by no workflow at all —
  intelligence, the assistant layer, **auth**, and the admin broker. The auth directory is
  19 of 19 unrun and is the tenancy and access layer.

## Layer Impact

- `global-control-lane`. One structure-map data file. No product surface, tenant data,
  schema, projection, migration, code path, or runtime behaviour. The map holds structure
  only, no status.

## Client Applicability

- All clients: no · Specific clients: none · Internal only: yes — operator tooling data
- Public/demo only: no · Feature flag: none

## Changes Included

- `scripts/exec/source-stage-map.json` — `T-404` … `T-409` placed.

## QA / Validation

| What | Result |
|---|---|
| Board run against live inputs | **exit 0** (was exit 1, six unmapped) |
| Claimable rows | **0 → 4** |
| `release-check` | passed |

No code changed. The mechanism that makes an unmapped id fail the board was added and
mutation-proofed separately; this is data satisfying it.

### Why no new test

Asserting that these six ids in particular are mapped would restate the gate against a
snapshot and need editing every time an id is filed — the hand-maintained-list shape this
repository keeps removing. The gate is the test.

## Rollout Plan

Merge to `main`. The next board run offers the four claimable items. No image build,
migration, flag, or runtime change.

## Deployment Authority

- Repo-owned deploy workflow: not exercised beyond the ordinary main deploy
- Shared runtime mutators: none · Approved image digest: not applicable
- ACA runtime invariant: not applicable · Worker image invariant: not applicable
- Feature/env flag update path: none · Live signed-in proof required: no

## Rollback Plan

Revert the PR. No data, migration, or runtime state. Reverting makes the six items invisible
again and fails the board run until they are replaced.

## Audit Evidence

- The PR diff — one data file.
- Board exit 1 with six unmapped before, exit 0 after; claimable rows 0 → 4.

## Known Gaps

- **Placing an item is not triaging it.** Nothing here says whether any of the six premises
  hold. Two of them make claims about deploy state that are unverified by this change, and
  one of those concerns a merge made from this session — so the agent placing them is not a
  neutral party on that pair and should say so when it takes them.
- Two of the six are not counted as claimable by the queue after placing, which is worth
  understanding before assuming all six are ready to pick up.
