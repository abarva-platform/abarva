# 2026-08-30-home-executive-opening-guard — Home Executive Opening Guard

## Release ID

`2026-08-30-home-executive-opening-guard`

## Status

`candidate`

## Plain-English Summary

This release tightens the Home executive-story generation path so the opening thesis must be a broad business consequence statement rather than a supplier ranking, contract fact, evidence-boundary caveat, standalone inventory count, narrow program update, or individual infrastructure-asset fact. The model prompt asks for that shape up front, and the deterministic story-plan selector refuses unsuitable openings if generation still produces them.

## Layer Impact

- Release lane: `global-control-lane`
- Products: Home preview/executive story rendering continues to read published Home chapter-claim and story-plan rows, but the story-plan opening selection is stricter.
- Source adapters / canonical model: No change.
- Projections: Home narrative writer rows are regenerated by the existing governed Home ECL narrative job.

## Client Applicability

- All clients: Applies to the Home ECL narrative writer and story-plan selector when Home executive story generation runs.
- Specific clients: None.
- Internal only: No.
- Public/demo only: No.
- Feature flag: Existing Home ECL/default provider behavior remains unchanged.

## Changes Included

- Updates the shared enterprise thesis prompt with an explicit business-led opening bar.
- Updates chapter assembly so supplier and evidence-boundary caveats cannot become Executive Brief opening claims.
- Updates the Home ECL story-plan selector so it does not fall back to the first unsuitable claim.
- Adds focused render/static tests for supplier, evidence-boundary, inventory-opening, narrow-program, and individual-asset guardrails.

## QA / Validation

- PASS — `npm run test:ecl-home-narrative-layer`
- PASS — `npm test -- --runTestsByPath src/components/home/v4/__tests__/HomeV4App.tier1.test.tsx --runInBand`
- PASS — `git diff --check`

## Rollout Plan

Merge to `main`, deploy through the repo-owned Azure Container Apps workflow, then run the governed Home ECL narrative apply job for the active assessment. Run independent readback and signed-in Home browser proof before calling the generated narrative live-proven.

## Deployment Authority

- Repo-owned deploy workflow: Required for the web runtime image.
- Shared runtime mutators: None outside the repo-owned deploy workflow and governed ACA data-build job.
- Approved image digest: To be captured by the deploy workflow.
- ACA runtime invariant: Required after deploy.
- Worker image invariant: Required after deploy.
- Feature/env flag update path: None.
- Live signed-in proof required: Yes, for Home preview after narrative apply/readback.

## Rollback Plan

Revert the PR, redeploy the prior digest through the repo-owned workflow, and rerun the Home ECL narrative apply job if the generated story-plan rows need to be restored from the previous writer behavior.

## Audit Evidence

- PR URL: https://github.com/abarva-platform/abarva/pull/7168
- CI/local tests: Commands above.
- ACA deploy run: To be filled after merge.
- ACA apply/readback runs: To be filled after apply.
- Browser proof: To be captured after apply/readback.

## Known Gaps

This release does not redesign every Home section or visual. It specifically closes the opening-thesis selection failure mode and keeps the broader Home V2 design work on the existing backlog.
