# 2026-09-21-execution-map-t468-t469 — Map New Execution-Control Items

## Release ID

`2026-09-21-execution-map-t468-t469`

## Status

`candidate`

## Plain-English Summary

Two newly filed internal execution items were present in the operator backlog but absent from the repository-owned structure map. That made the generated board fail closed and prevented the queue from being rebuilt.

This release places both items on non-lifecycle tracks only. It does not decide either item and does not implement product behavior.

## Layer Impact

- Release lane: `internal-admin`.
- Layers 1 through 4: no change.
- Internal operator tooling: the Source execution board and queue can account for the new backlog items without inflating Source lifecycle progress.

## Client Applicability

- All clients: no product or data behavior changes.
- Specific clients: none.
- Internal only: all operators using the generated execution board and queue.
- Public/demo only: no.
- Feature flag: none.

## Changes Included

- Map `T-468` to the outside-lifecycle track because it is a Tower/control decision item, not Source lifecycle progress.
- Map `T-469` to the platform-integrity track because it is test/tooling data-plane safety work.
- Add behavior coverage proving the exact placements and proving the board fails closed if either mapping is removed.

## QA / Validation

- PASS before/after defect measurement: running the real board generator against `/Users/anand/Downloads` with the `origin/main` map exited `1` and reported `not placed on the map: 2 -> T-468, T-469`.
- PASS real board regeneration: `SOURCE_EXECUTION_HOME=/Users/anand/Downloads node scripts/exec/build-source-board.mjs --json` exited `0` and reported `not placed on the map: 0`.
- PASS real queue regeneration: `SOURCE_EXECUTION_HOME=/Users/anand/Downloads node scripts/exec/build-execution-queue.mjs` exited `0` and wrote `EXECUTION_QUEUE.md` with 7 claimable rows.
- PASS focused queue behavior: `node scripts/exec/build-execution-queue.test.mjs` reported 35 passed / 0 failed.
- PASS local gates: syntax check, focused ESLint, TypeScript, whitespace, and `npm run release:check`.

## Rollout Plan

Squash-merge through the protected pull-request path. Operators receive the corrected board and queue behavior when they regenerate from the merged map.

## Deployment Authority

- Repo-owned deploy workflow: `.github/workflows/aca-main-deploy.yml`; no ad hoc deployment.
- Shared runtime mutators: none in this change.
- Approved image digest: recorded by the repo-owned workflow if a deployment runs.
- ACA runtime invariant: read-only verification if deployed.
- Worker image invariant: read-only verification if deployed.
- Feature/env flag update path: none.
- Live signed-in proof required: no; this change has no product surface.

## Rollback Plan

Revert the squash merge and regenerate the private operator board and queue. No tenant data, schema, migration, runtime, or feature flag rollback is required.

## Audit Evidence

- `scripts/exec/source-stage-map.json`
- `scripts/exec/build-execution-queue.test.mjs`
- `/Users/anand/Downloads/source-board-summary.json`
- `/Users/anand/Downloads/EXECUTION_QUEUE.md`
- Pull request and required-check results

## Known Gaps

This change only restores execution-control accounting. `T-468` remains a decision-needed item, and `T-469` remains implementation work for future data-plane safety tooling.
