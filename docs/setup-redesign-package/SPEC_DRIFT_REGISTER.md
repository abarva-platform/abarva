# Spec Drift Register
## Setup Redesign Package · 3-PR run

Per `SETUP_REDESIGN_PACKAGE_2026-05-07.md` §1.7. One entry per drift: where the spec was wrong, ambiguous, or required deviation.

| Entry | PR | Spec section | What spec said | What we did | Why |
|---|---|---|---|---|---|
| 1 | All | Catalog "Common substrate references" | Refers to a `tenants` table with `display_name`, `industry_classification`. | Used `clients` table (existing) via `getActiveClientRow()` per Setup Fix Package PR 2. `industry_classification` doesn't exist; mapped from `clients.industry_code` (`HEALTHCARE_IDN` / `FINSERV` / `RETAIL` / `ENERGY`) to plain-language phrase via static map. | Substrate vocabulary mismatch between catalog and shipped state. Logged once, applies across all 3 PRs. |
| 2 | All | Catalog `data_inventory_segments` columns | Catalog uses `health_status` with values `decision-grade / usable evidence / partial / thin / empty / not_loaded` and `coverage_pct` (0-1). | Substrate uses `health_state` with values `complete / partial / sparse / not_started / attention / critical` and `coverage_score` (0-100). Created a thin translation lib (`src/lib/admin/setup-vocab.ts`) that maps substrate vocabulary ↔ catalog vocabulary in one place. | Centralizes the translation so PR B and PR C don't duplicate the mapping. Translation rules in `setup-vocab.ts`. |
| 3 | All | Catalog 23-segment "Wave 4" references | "23 segments post-Wave 4", "Tier 2-4 enrichment segments 17-23". | Substrate constraint is `family_number BETWEEN 1 AND 14`. Treated 14 as the canonical universe; segments 15-23 are future. Trust ladder collapsible expand says "all 14" not "all 23". | 23-segment world doesn't exist on `main`; treating it as future in fallback copy. |
| 4 | PR A | Block 1.3 action queue uses `tenant_settings.sso_configured` | Catalog assumes queryable `tenant_settings` table. | `tenant_settings` doesn't exist. Used the same hardcoded `ssoConfigured: false` constant from `users-access-page-view.ts` (already shipped in Fix Package PR 5). | Substrate not present; flagged as future work. |
| 5 | PR A | Block 1.3 connector decisions | Catalog: query `connectors` for `state IN ('decision_pending', 'awaiting_review')`. | No such queryable state on `connectors` substrate. Omitted connector items from Overview action queue. | Per catalog fallback ("if can't be queried, omit"). |
| 6 | PR A | §3 "components stay in codebase" | "Act 1 cards, matrix, landscape, Act 3 templates stay; PR B/C absorb." | Refactored existing components (`SetupSentinelOpener`, `SetupCapabilityMatrix`, `SetupRecentActivity`, `DataLandscapeTable`, `SetupActOne`, `SetupActThree`) directly. Some get renamed/relocated in PR A; their data feeds PR B and PR C. The "delete or keep" call is per-component based on consumer analysis. | The package's "Act 1 cards" naming doesn't map 1:1 to shipped components. Consolidated decision: keep the data registry, rewire compositions. |
| 7 | PR A | Spec assumes new components | "Create StatusHeader, StewardOrientation, ActionQueue, RecentActivity in `src/components/admin/overview/`." | Created the 4 components but `StewardOrientation` reuses the prose-generation logic from `SetupSentinelOpener` (already shipped). `RecentActivity` reuses filtering pattern from existing `SetupRecentActivity`. | Don't recreate what exists; rename + relocate. |
| 8 | All | Browser-Chrome QA gate | "Sign in as FCF admin and verify panels visually." | Local preview can't get past Clerk keyless mode (verified during Fix Package). Used local route smoke tests (200 status + console-error checks via dev server logs) as the gate; Vercel preview is the visual verification target — flagged for the human to verify. | Clerk auth wall blocks programmatic browser QA in this worktree. |
