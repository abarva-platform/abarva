# AbarVa Strategic Backlog — 2026-05-14

> **How to read this.** Four parallel workstreams. No time estimates — focus is on parallelizability and dependencies. Items marked `IN FLIGHT` are partially shipped from the 2026-05-13 audit arc (PRs #1923–#1933). The narrative work (D1) frames everything downstream and should be locked first.

---

## A · Engineering (highest priority per founder)

| ID | Item | Status | Notes |
|---|---|---|---|
| A1 | **End-to-end testing coverage** | open | Top priority. Login → Intelligence → Source → Moves → Tower per tenant. The 8 SEC-P0 cross-tenant probes from the audit B-agent should land as a permanent regression suite. |
| A2 | **Context layer stand-up + refresh ergonomics** | partial · IN FLIGHT | V4 Brief panels now accept tenant-keyed data (PR #1923). First Capital seed pack complete (PR #1929). Need: one-command bootstrap for a new tenant. "Refresh from upstream" doctrine: how does a customer pull a fresh industry corpus + apply tenant overrides? |
| A3 | **Platform-wide vs tenant-specific feature gating** | partial · IN FLIGHT | Tenancy guards live on 8 cross-tenant routes; locked-role pinning closed at resolver (PR #1930). Need: explicit contract for what ships to all tenants vs what's pinned per client. Feature-flag system if not already in place. |

---

## B · Azure infrastructure (research → design docs, parallel to A)

| ID | Item | Status | Notes |
|---|---|---|---|
| B1 | **SaaS architecture decision** | open | Three-tier segregation: (a) application tier (b) per-client data plane (c) industry corpus / shared substrate. Design doc with diagrams. |
| B2 | **Demo private planes for 3 dummy clients** | open | Apex / Meridian / First Capital as a reference deployment that proves B1. Demonstrable to a customer's sec team without showing real customer data. |
| B3 | **SaaS vs corporate Azure setup** | open | Landing zone yes/no, subscription model (per-environment vs per-customer), network topology, identity boundary. |
| B4 | **Client VPC deployment** | open | One-time setup + maintenance cost model. Which control plane stays ours; which moves to theirs. Build vs lift-and-shift. |
| B5 | **PHI/PII prevention guardrails** | open | Ingest-time filtering. Candidates: Microsoft Purview, Azure Information Protection labels, content-scan webhook before write. Explicit scope decision: what is the client context layer *allowed* to hold by design? Is anything sensitive in scope? If not by design, what enforces "not"? |

---

## C · Pilot operations (planning can start now)

| ID | Item | Status | Notes |
|---|---|---|---|
| C1 | **First-pilot deployment runbook** | open | Step-by-step from "signed SOW" to "first CXO logged in". Depends on A1 + B-cluster. |
| C2 | **Support model** | open | Do we need a team? What's the smallest viable shape? (likely: founder-led incident, contracted SRE for pager rotation, one DPA/sec analyst for customer audits.) |
| C3 | **Sustaining pilot** | open | Who carries the pager, what gets escalated, SLA we can commit to without burning out a 1-2 person company. |
| C4 | **Infosec approval accelerator** | open | The biggest pilot blocker. Output: a checklist of features/controls that unlocks corporate sec review. Likely answer: client VPC deployment is the feature that unlocks the most accounts. |

---

## D · Business / GTM (fully independent of engineering)

| ID | Item | Status | Notes |
|---|---|---|---|
| D1 | **Product narrative + value proposition** | open · GATING | Strengthen and lock. Frames D2, D6, C4 sales conversations. One-pager + 3-slide deck. Done when a CXO can repeat it back. |
| D2 | **Monetization** | open | Tier structure (Pilot / Production / Enterprise?) + ARR model. What's included per tier. Per-tenant vs per-seat vs platform-license. |
| D3 | **Patent decision** | open | Yes / defensive publication / pass. This is a decision, not a project. Inputs: provisional cost ~$3-5k, full ~$15-25k, defensive publication ~$0-1k. |
| D4 | **Company setup cost** | open | Registration + legal + insurance + bookkeeping. Projected over 3–6 months. Likely: Delaware C-corp formation, founder agreements, customer DPA template, E&O + cyber insurance, basic accounting. |
| D5 | **Startup credits** | open · quick-win | Apply to both Azure for Startups ($25k–$150k Azure credit) and Anthropic Founder Credits. Each is a 1–2 hour application. Pure upside. |
| D6 | **Seed funding plan** | open | Thesis, ask, use of proceeds, intent. Depends on D1 (narrative) and D2 (monetization) being solid. |

---

## Quick wins (no dependencies, can knock off in parallel)

- **D3** patent yes/no — pure decision
- **D5** Azure + Anthropic credit applications — application forms
- **D1** narrative one-pager — gates everything downstream
- **B5** PHI/PII guardrail design doc — gates infosec conversations
- **C4** infosec approval checklist — gates pilot conversations

---

## Critical dependencies

```
D1 (narrative) ──┬──> D2 (monetization) ──> D6 (seed funding plan)
                 └──> C4 (infosec story)

B5 (PHI/PII)  ──> C4 (infosec accelerator) ──> C1 (pilot runbook)
B1 (architecture) ──> B2 (demo private planes) ──> C1
A1 (E2E coverage) ─────────────────────────────> C1, C2, C3
```

---

## Already in flight from the 2026-05-13 audit

- **A2 partial** — V4 Brief panels accept tenant-keyed data; First Capital seed pack complete (PRs #1923, #1929)
- **A3 partial** — Locked-role tenant resolution closed at the resolver layer (PR #1930)
- **B5 foundation** — Tenancy guards on all 8 cross-tenant routes; `requireTenancy()` is the canonical pattern; Clerk JWT template verified emits `tenant_key`/`role`/`sub` (PR #1923 + ops)
- **A1 foundation** — 8 SEC-P0 cross-tenant routes ready to become a curl-based regression suite (PR #1923 + audit B-agent's pen-test playbook)

---

## Out of scope for this backlog

- Agent C architecture refactor (`types.ui.ts`/`types.db.ts` split for 11 modules, full transformer rollout) — ~15-17 engineer-days, multi-PR. Tracked separately.
- Full page-by-page visual redesign — beyond the targeted polish in PR #1928.
- Sentinel prompt-tuning beyond arithmetic guard (PR #1932 closed the audit-flagged item).

---

*Updated 2026-05-14. New items append below; date-stamp the entry. Status transitions: `open` → `in flight` → `done`. Reference PR numbers when an item ships.*
