# AbarVa Strategic Backlog — 2026-05-14

> **How to read this.** Four parallel workstreams plus one cross-cutting concern (Data ingestion model). No time estimates — focus is on parallelizability and dependencies. Items marked `IN FLIGHT` are partially shipped from the 2026-05-13 audit arc (PRs #1923–#1933) and follow-on work (#1934+). The narrative work (D1) frames everything downstream and should be locked first.

> **Revision 2026-05-14b:** A2 split into a/b/c sub-items to reflect the actual seed → broker → UI pipeline (14 seed segments → 15 coverage tiles + 6 synthesized cards on `/intelligence#enterprise-context`). B5 split similarly and re-statused — pattern-based sensitive-upload guard already shipped (`src/lib/security/sensitive-upload-guard.ts`) and wired into 5 of 7 upload routes. New section: **Data ingestion model (four-tier)**.

---

## A · Engineering (highest priority per founder)

| ID | Item | Status | Notes |
|---|---|---|---|
| A1 | **End-to-end testing coverage** | open | Top priority. Login → Intelligence → Source → Moves → Tower per tenant. The 8 SEC-P0 cross-tenant probes from the audit B-agent should land as a permanent regression suite. |
| **A2** | **Context layer stand-up + refresh ergonomics** | **partial · IN FLIGHT** | Split into A2a/A2b/A2c. The context layer has three rendering layers: 14 seed segments (folder structure) → broker-processed rows → 15 coverage-by-domain tiles + 6 synthesized cards on `/intelligence#enterprise-context`. Bootstrap/refresh must cover all three. |
| ↳ A2a | **Tier-1 ingestion: UI upload + sensitive-upload guard** | partial · IN FLIGHT | 5/7 upload routes already call `evaluateSensitiveUpload` (`data/upload`, `tower/upload`, `admin/upload-dataset`, `programs/[id]/attachments/upload`, `v1/source/[eventId]/artifacts/upload`). **2 routes still need wiring:** `programs/workspace/[moveId]/upload`, `v1/nexus/upload`. |
| ↳ A2b | **Tier-2 ingestion: Azure landing zone per tenant** | open | Bicep module for a per-tenant Azure Blob Storage container (private endpoint, customer-controlled SAS / service principal). Scheduled pickup job that runs the broker pipeline. Default channel for recurring structured drops (KPI snapshots, financial extracts, contract registry, telemetry). |
| ↳ A2c | **`tenant:bootstrap` + `tenant:refresh` scripts** | open | Wrap the full seed → broker → UI pipeline into two npm scripts. `bootstrap <key>` provisions Clerk + Supabase + JWT pin + 14-segment load + broker rebuild + verify all 15 tiles + 6 cards render. `refresh` re-pulls corpus while preserving tenant overlays. README + verification report. |
| A3 | **Platform-wide vs tenant-specific feature gating** | partial · IN FLIGHT | Tenancy guards live on 8 cross-tenant routes; locked-role pinning closed at resolver (PR #1930). Need: explicit contract for what ships to all tenants vs what's pinned per client. Feature-flag system if not already in place. |

---

## B · Azure infrastructure (research → design docs, parallel to A)

| ID | Item | Status | Notes |
|---|---|---|---|
| B1 | **SaaS architecture decision** | open | Three-tier segregation: (a) application tier (b) per-client data plane (c) industry corpus / shared substrate. Design doc with diagrams. **Pin Azure Database for PostgreSQL Flexible Server** as the data-plane SKU before the Codex lab branches further. |
| B2 | **Demo private planes for 3 dummy clients** | open | Apex / Meridian / First Capital as a reference deployment that proves B1. Demonstrable to a customer's sec team without showing real customer data. |
| B3 | **SaaS vs corporate Azure setup** | open | Landing zone yes/no, subscription model (per-environment vs per-customer), network topology, identity boundary. |
| B4 | **Client VPC deployment** | open | One-time setup + maintenance cost model. Which control plane stays ours; which moves to theirs. Bicep parameter files per customer. |
| **B5** | **Sensitive-data prevention guardrails** | **partial · IN FLIGHT** | Split into B5a/B5b/B5c. Pattern-based guard shipped (`src/lib/security/sensitive-upload-guard.ts`) with 5 classification levels (`public` / `internal` / `confidential_business` / `restricted_financial` / `regulated_phi_pii_suspected`) and allow/quarantine decision. Wired into 5 of 7 upload routes. |
| ↳ B5a | **Wire guard to remaining 2 upload routes** | open · punch-list | `programs/workspace/[moveId]/upload` and `v1/nexus/upload` don't call `evaluateSensitiveUpload`. Small fix; same import + call pattern as the other 5. |
| ↳ B5b | **Microsoft Purview content-scan integration** | open | Replace / augment pattern-based detection with Purview-driven classification. Richer accuracy, named DLP policies, customer-recognizable enterprise control. Shared service across all ingestion tiers (A2a UI, A2b Azure blob, A2c bootstrap). |
| ↳ B5c | **Quarantine + audit dashboard** | open | Admin UI: list of quarantined uploads with reason, who tried to upload, ability to release after manual review (or hard-delete). Stream to SIEM-friendly audit log. |

---

## C · Pilot operations (planning can start now)

| ID | Item | Status | Notes |
|---|---|---|---|
| C1 | **First-pilot deployment runbook** | open | Step-by-step from "signed SOW" to "first CXO logged in". Depends on A1 + B-cluster. |
| C2 | **Support model** | open | Do we need a team? What's the smallest viable shape? (likely: founder-led incident, contracted SRE for pager rotation, one DPA/sec analyst for customer audits.) |
| C3 | **Sustaining pilot** | open | Who carries the pager, what gets escalated, SLA we can commit to without burning out a 1-2 person company. |
| C4 | **Infosec approval accelerator** | open | The biggest pilot blocker. Output: a checklist of features/controls that unlocks corporate sec review. Lead with B4 (client VPC) + B5 (sensitive-data guard) + A3 (tenancy isolation). Pre-fill standard CAIQ / SIG-lite. |

---

## D · Business / GTM (fully independent of engineering)

| ID | Item | Status | Notes |
|---|---|---|---|
| D1 | **Product narrative + value proposition** | open · GATING | Strengthen and lock. Frames D2, D6, C4 sales conversations. One-pager + 3-slide deck. Done when a CXO can repeat it back. |
| D2 | **Monetization** | open | Tier structure (Pilot / Production / Enterprise) + ARR model. What's included per tier. Per-tenant vs per-seat vs platform-license. |
| D3 | **Patent decision** | open | Yes / defensive publication / pass. This is a decision, not a project. Inputs: provisional cost ~$3-5k, full ~$15-25k, defensive publication ~$0-1k. |
| D4 | **Company setup cost** | open | Registration + legal + insurance + bookkeeping. Projected over 3–6 months. Likely: Delaware C-corp formation, founder agreements, customer DPA template, E&O + cyber insurance, basic accounting. |
| D5 | **Startup credits** | open · quick-win | Apply to both Azure for Startups ($25k–$150k Azure credit) and Anthropic Founder Credits. Each is a 1–2 hour application. Pure upside. |
| D6 | **Seed funding plan** | open | Thesis, ask, use of proceeds, intent. Depends on D1 (narrative) and D2 (monetization) being solid. |

---

## ☆ Data ingestion model (cross-cutting · informs A2, B4, B5, C4)

Different segments have different sensitivity and refresh cadence, so they get different ingestion paths. Enterprise IT expects a tiered answer, not "we have an upload button."

| Tier | Channel | Use case | When |
|---|---|---|---|
| **1. App upload** | AbarVa UI (browser file picker) | Ad-hoc: policy doc, single vendor contract, org chart update, exec interview transcript | Pilot default for low-volume / unstructured / one-off content |
| **2. Azure landing zone** | Per-tenant Azure Blob container; customer drops via Azure Storage Explorer / AzCopy / their ETL job | Recurring structured data: KPI snapshots, program inventory, IT financials, contract registry, operating telemetry | Production default for weekly/monthly refresh |
| **3. Direct integration** | Azure Data Share, Snowflake Data Share, point connector (ServiceNow, Workday, Coupa) | Live operational signals: incidents, change tickets, vendor renewals | Enterprise tier — when trust is established |
| **4. In-VPC ingestion** | AbarVa stack deployed into customer's Azure subscription (B4); data never leaves their tenancy | Regulated industries (healthcare, banking, defense) | Required for some accounts before they'll sign |

### Shared controls (apply to every tier)

| Control | Pilot baseline | Production target |
|---|---|---|
| TLS in transit | TLS 1.2+ | TLS 1.3 + mTLS on Tier 2-4 |
| Encryption at rest | AES-256 default | Customer-managed keys via Key Vault |
| Authentication | SAS token with TTL / service principal | Entra ID federation; no shared keys |
| Audit log | Every ingest event logged | Immutable, SIEM-streamable |
| Sensitive-data gate | Pattern-based `sensitive-upload-guard` (shipped, B5a) | + Purview classification (B5b) |
| Retention / deletion | Customer-deletable via support | Customer-deletable via UI; auditable retention windows per segment |
| Network isolation | Public endpoint with TLS for Tier 1 | Private endpoint for Tier 2+; VNet peering for Tier 3 |
| Quarantine | Failed-scan files → quarantine container, surfaced to admin (B5c) | Same + auto-delete after retention window |

### Recommended pilot default

**Tier 1 + Tier 2 combo.** Tier 1 (UI upload) handles 5 segments that arrive as unstructured docs (`enterprise_profile`, `compliance`, `industry_context`, `program_deliverables`, ad-hoc evidence). Tier 2 (Azure landing zone) handles the 9 segments that need recurring batch refresh. Both paths feed the same broker pipeline, same sensitive-data scan, same audit log, same per-tenant data plane.

---

## Quick wins (no dependencies, can knock off in parallel)

- **D3** patent yes/no — pure decision
- **D5** Azure + Anthropic credit applications — application forms
- **D1** narrative one-pager — gates everything downstream
- **B5a** wire sensitive-upload guard to the remaining 2 upload routes — small punch-list
- **C4** infosec approval checklist — gates pilot conversations

---

## Critical dependencies

```
D1 (narrative) ──┬──> D2 (monetization) ──> D6 (seed funding plan)
                 └──> C4 (infosec story)

B5a (guard wiring) ──> B5b (Purview) ──> C4 (infosec accelerator) ──> C1 (pilot runbook)
B1 (architecture) ──> B2 (demo private planes) ──> C1
A2b (Azure landing zone) ──> A2c (bootstrap script) ──> C1
A1 (E2E coverage) ─────────────────────────────────> C1, C2, C3
```

---

## Already in flight from the 2026-05-13 audit + follow-on

- **A2 partial** — V4 Brief panels accept tenant-keyed data; First Capital seed pack complete (PRs #1923, #1929)
- **A3 partial** — Locked-role tenant resolution closed at resolver layer (PR #1930)
- **B5 partial** — Sensitive-upload guard shipped (`sensitive-upload-guard.ts`) and wired into 5 of 7 upload routes
- **B-foundation** — Azure scale-test foundation landed (PR #1938)
- **A1 foundation** — 8 SEC-P0 cross-tenant routes ready to become a curl-based regression suite (PR #1923 + B-agent's pen-test playbook)

---

## Out of scope for this backlog

- Agent C architecture refactor (`types.ui.ts`/`types.db.ts` split for 11 modules, full transformer rollout) — ~15-17 engineer-days, multi-PR. Tracked separately.
- Full page-by-page visual redesign — beyond the targeted polish in PR #1928.
- Sentinel prompt-tuning beyond arithmetic guard (PR #1932 closed the audit-flagged item).

---

*Updated 2026-05-14 (rev b). New items append below; date-stamp the entry. Status transitions: `open` → `partial · IN FLIGHT` → `done`. Reference PR numbers when an item ships.*
