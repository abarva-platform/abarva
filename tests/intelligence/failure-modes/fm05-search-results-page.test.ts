/**
 * FM #5 — Search-results page · INT-RGS (placeholder)
 *
 * Failure mode: J3 conversational responses render as bulleted
 * pattern lists rather than synthesis prose.
 *
 * All assertions here require LLM output to evaluate. Land in
 * CB-6 alongside the post-hoc validator.
 */

describe('FM #5 — Search-results page (placeholder)', () => {
  it.todo(
    'real Sentinel response on a J3 question is synthesis prose, not a bulleted list (LLM-dependent — CB-6)',
  );
  it.todo(
    'response paragraph count > 1 and bullet-list count is 0 or 1 (CB-6)',
  );
  it.todo(
    'response weaves citations inline rather than appending them as a list (CB-6)',
  );
});
