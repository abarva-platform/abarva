import type { CioTowerMetricPacket } from '@/lib/cio-tower/metric-packet';

export async function loadCioTowerMetricPackets(tenantKey: string | readonly string[]): Promise<CioTowerMetricPacket[]> {
  void tenantKey;
  return [];
}
