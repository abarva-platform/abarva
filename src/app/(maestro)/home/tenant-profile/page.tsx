// /home/tenant-profile · canonical Home panel.
// Re-exports /admin/tenant.
export { default, metadata } from '../../admin/tenant/page';

export const dynamic = 'force-dynamic';
export const revalidate = 0;
