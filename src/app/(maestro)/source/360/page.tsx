import SourceWorkspacePage, {
  metadata as workspaceMetadata,
} from "../workspace/page";

export const metadata = {
  ...workspaceMetadata,
  title: "Source 360 · AbarVa",
};

export const dynamic = "force-dynamic";

export default SourceWorkspacePage;
