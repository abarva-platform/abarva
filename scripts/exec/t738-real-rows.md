<!-- frozen 2026-09-23 from EXECUTION_BACKLOG_20260918.md; do not reflow -->

<!-- T-066 | source line 4748 | expect lane T -->
| T-066 | **A bare side-effect import is not read as a governed edge, so a test that loads a governed module only that way scores zero.** `importedProductSources` collects specifiers with `/(?:\bfrom\s*\|\bimport\s*\(\s*\|\brequire\s*\(\s*)["']([^"']+)["']/g` plus one *inferred sibling* (`a/__tests__/b.test.ts` → `a/b.*`). `import "../route";` matches neither branch of the regex, so it contributes nothing. **Demonstrated, not inferred:** a fixture directory whose only edge was `import "../route";` to a module containing `approve(` scored 0 and was banded `unclassified`; changing it to `import { POST } from "../route";` scored 100 / `critical` with the same module. The three governed directories in the pre-existing ranking case all score via the inferred sibling, so nothing currently depends on the gap — it under-states governed risk, which is the dangerous direction, and it is the same class of error the census's own header warns about | T | Add the side-effect form to the specifier pattern (`import "x"` with no clause) and re-measure the ranking. Report how many directories change band — if the number is large the ranking order changes again and that is the point, not a reason to defer. Do **not** widen it to any quoted string in the file: the census already refuses to credit a path a reachable script merely *mentions*, and this must not re-introduce that error on the import side |

<!-- T-071 | source line 4992 | expect lane T -->
| T-071 | **The mandatory PR hygiene gate reports TypeScript clean by crashing, passes a failing secret-hygiene suite, and has two branches that cannot fail.** Four defects in `scripts/integration/hygiene_gate.sh`, which `hygiene-gate.yml` runs on every PR. **(1) Section 4 is the exact false-clean this backlog's standing rules warn every author about, inside the gate itself**: `npx tsc --noEmit --pretty false 2>&1 \| grep -q "error TS"` with no heap option. Proven by execution on `e4b17755a` — the command **exits 134** with a V8 out-of-memory stack trace and **zero** `error TS` lines, so `grep` exits 1 and the gate prints `[PASS] TypeScript clean`. A real type error would be reported identically. **(2) Section 3 matches jest's summary with `grep -q "Tests:.*passed"`, which a partially failing run satisfies.** Proven by running the gate's own pipeline against a known-red suite: `Tests: 8 failed, 53 passed, 61 total` → the gate prints `[PASS] Secret hygiene tests passed`. **(3) Two vacuous-pass branches** — a missing `build-waves.json` and a missing secret-hygiene test file each print `pass "... not present (skipped)"`, the item-47 class, and one of them is the disclosure check. **(4) Section 6's failure branch is also a `pass`** (`"Stash check complete (review output manually)"`), so a risky stash cannot fail the gate | T | Judge exit codes, not greps: the typecheck runs at the documented heap and its exit status decides; the jest check reads the run's exit status. A declared subject that is missing fails rather than skips. No check has two passing branches. A behavioral suite drives the real script and each repair is mutation-checked — in particular the typecheck one must fail when the heap option is removed, which is the mutation that reproduces today's defect |

<!-- T-423 | source line 6978 | expect lane C -->
| T-423 | **Source (b) is exhausted at the surface level, and the figure carried in the last two pulses does not reproduce.** Read directly from `docs/security/ai-surface-control-catalog.json` on `origin/main`: all 18 surfaces' `requiredControls` now carry a `behavioralTest` — **zero** without one. What remains uncovered is a different register: of 37 `catalogClaimCoverage` rows, 15 are `covered` and **22 are `deferred`**, and not one deferred row carries a `surfaceId`, so none is bound to a catalog surface at all. The prior pulses report "6 uncovered, all on two `routeReachable: false` components"; the catalog file has not changed since 2026-09-19 14:53 CDT, so the difference is method, not content, and this watcher cannot reconcile it without the earlier probe. The highest-consequence deferred row is **`consequential\|Intelligence\|Gate waiver / approval\|human-approval-gate`** — a waiver gate with no surface binding and no test | C | Decide per deferred row whether it names a real surface or is an aspiration. For the Intelligence gate-waiver row specifically: bind it to a catalog surface with a `path` and a `behavioralTest` that executes the real handler and fails when the gate is removed, or retire the claim row with a stated reason. **Do not mark a row `covered` by pointing at an adjacent test** — item 25 exists because a token appearing in executable code is not proof the code is reached. Start by settling whether a `surfaceId`-less row is permitted by the schema at all; if it is not, that is its own defect |

<!-- T-429 | source line 7048 | expect lane C -->
| T-429 | **The two highest-consequence deferred control rows are the only `consequential` ones left, and neither is bound to a surface.** Read from `docs/security/ai-surface-control-catalog.json` on `origin/main` this run: 37 `catalogClaimCoverage` rows, **21 deferred** — down one from the 22 the last pulse recorded, which is T-423 closing the Intelligence gate-waiver row, so that register is now moving. Nineteen of the 21 are `generated-ui`; exactly two carry the `consequential` severity, and the higher of them is **`consequential\|Source\|Vendor-facing artifact renders/exports\|human-approval-gate`**, whose own stated reason is that "artifact routes vary and send/approval enforcement is not universal enough". This is the gate on what leaves the building to a counterparty, and it is deferred with no `surfaceId` | C | Establish which artifact routes actually enforce a completed human approval before a vendor-facing render or export, and which do not — enumerate them, do not sample. Then either bind the row to a catalog surface with a `path` and a `behavioralTest` that executes the real export handler and fails when the approval gate is removed, or narrow the claim to the routes that genuinely enforce it and say so. **Do not mark it `covered` by pointing at the export watermark** — the watermark is a label on output, not a gate on the decision, and item 25 exists because a token appearing in executable code is not proof the code is reached. The second row, `consequential\|Tower\|Portfolio data upload/ingest\|human-approval-gate`, is deliberately left for a separate item; its reason says the claim is access/quarantine coverage rather than a decision gate, which is a different defect |

<!-- T-431 | source line 7103 | expect lane C -->
| T-431 | **The last `consequential` deferred catalog claim, and the one this register has been deferring around.** Of the 21 deferred `catalogClaimCoverage` rows read from `origin/main` this run, 19 are `generated-ui` and exactly two carry `consequential` severity. T-429 took the higher one (Source, vendor-facing renders/exports) and explicitly left the second for a separate item. This is that item: **`consequential\|Tower\|Portfolio data upload/ingest\|human-approval-gate`**, whose own stated reason is that the "catalog claim is access/quarantine coverage, not a completed human decision gate for AI-derived action." The claim as written asserts a human approval gate on portfolio data entering Tower; the reason concedes the evidence is really tenant access control and quarantine | C | Establish what actually stands between an uploaded or ingested portfolio dataset and its use in an AI-derived Tower surface — enumerate the ingest entry points, do not sample one. Then either bind the row to a catalog surface with a `path` and a `behavioralTest` that executes the real ingest handler and fails when the approval gate is removed, or narrow the claim to what access/quarantine genuinely covers and say plainly that no completed-decision gate exists. **Do not mark it `covered` by pointing at RLS, tenant fencing, or a quarantine flag** — the row's own reason already names those as the thing being mistaken for a gate; access control decides *who may*, an approval gate decides *whether it has been agreed*, and item 25 exists because a token appearing in executable code is not proof the code is reached. Tower numbers stay deterministic: this is a gate on ingest, not a change to any metric path |

<!-- T-455 | source line 7348 | expect lane C -->
| T-455 | **A phase the product displays has no lifecycle completion contract behind it, and the suite that would have said so had never run.** `src/lib/programs/programs-detail-view.ts` renders a "P6 Tower Handoff" workbench (line 156), a generic preview for programs without a flagship override (line 224), a gate panel (line 486) and a phase panel (line 542); `programs-page-view.ts:32` calls the canonical model "7-phase P0–P6", and `program-instance.ts:9` says the same. The phase-pack registry authors **six**: `PACKS` in `phase-packs/index.ts` holds 0–5, `PACKS_V2` holds 0–5, `getPhasePack` returns null above 5, and `PhaseNumber` is `0 \| 1 \| 2 \| 3 \| 4 \| 5` in both `types.ts` and `types.v2.ts`. So `buildProgramLifecycleContract` cannot produce a seventh contract, and the one case that asserted it — `lifecycle-operating-system.test.ts` — was **red from the day it landed** and ran in no workflow, so nothing could report it. T-440 corrected the expectation to six and recorded the gap; it did not close it | C | **Decide first, do not code first.** Either P6 is a real Programs phase, in which case it needs a phase pack, a `PhaseNumber` that admits 6, and a lifecycle completion contract — or it is deliberately outside the Programs lifecycle because it hands execution monitoring to Tower, in which case the **product vocabulary is what is wrong** and four render sites plus three comments describe a lifecycle the model does not have. **Do not resolve it by deleting the P6 render paths**: a phase an operator can see today disappearing is a product change with its own owner. State which it is in one paragraph, mark `decision needed`, and move on. **Recommendation:** the second. `nexus-program-workbench-view.ts:143` already says "P6 hands monitoring to Tower", and Tower owning steady-state monitoring is the operating model elsewhere in the product — so P6 reads as a handoff *out* of the Programs lifecycle rather than a seventh phase *of* it, and the honest repair is to say so at the four render sites rather than to author a seventh pack. That is still a call for the product owner, not for an executor |

<!-- T-468 | source line 7697 | expect lane C -->
| T-468 | **A tenant-scoping input is accepted and silently ignored, and nothing would tell a caller who used it.** `buildAtlasTowerCurrentState` (`src/lib/atlas/tower-grounding.ts:325`) declares `tenantKeyCandidates?: readonly (string \| null \| undefined)[]` in its input type. The body **never reads it** — the tenant key it actually scopes by is `row?.tenant_key ?? row?.slug ?? fallbackTenantKey` — and its only call site (`src/lib/atlas/tool-belt.ts:53`) does not pass it. No caller is harmed today, which is exactly why it is a trap rather than a bug: `tenantKeyCandidates` is a **live, load-bearing concept elsewhere** (`readTowerCommandCenter.ts:1061`, `current-layer-answer.ts:22`, and both Tower chat/ask routes build and pass one), so a future caller has every reason to expect supplying it scopes the read. It would be accepted, ignored, and the read would silently fall back to whatever the client row says. Found while executing T-466, which deliberately did **not** remove it: a test-only change is the wrong place to make this call | C | **`decision needed` — do not guess.** Two defensible outcomes and they are not equivalent: **(a)** the field is vestigial from an abandoned V7 projection wiring (no `loadV7TowerProjection` exists anywhere in `src/`), so delete it and let the type stop lying; or **(b)** the omission is the defect — this function *should* honour caller-supplied candidates the way the sibling Tower readers do, in which case wire it through and the divergence is a real tenant-scoping inconsistency across Tower read paths. **Recommendation: (a).** No caller passes it, no code reads it, and the module it was built for does not exist; (b) invents a requirement nobody has asked for and widens a tenant-scoping surface on speculation. If (a), the deletion must be type-only and the T-466 suite's tenant-scoping cases must still pass unchanged — they assert scoping through the row, which is the behaviour either way. Whoever takes it: the suite already pins the current contract, so this is now a safe change to make rather than a risky one |

<!-- T-510 | source line 8297 | expect lane T -->
| T-510 | **A claim line's item id is whichever id happens to be written first, so a line can release an item it never names.** `parseClaimRecord()` in `build-execution-queue.mjs` takes the id from the FIRST `item <id>` on the line, falling back to `CLAIM(ED) <id>`. Neither grammar covers the release form the register actually uses when the id leads the sentence. Found while closing T-545, on a live line: `- 2026-09-20T03:13Z \| claude-code-interactive \| RELEASED T-005 \| MERGED PR #8013 ... item 126 ...` — the subject is `T-005`, written without the `item` prefix, so the parser never sees it and attributes the whole line to `item 126`, 251 characters later. T-545 stopped that line releasing `126` (correctly), which means it now releases **nothing**: `T-005`'s release is silently unrecorded, and an item that WAS released reads as still held. That error direction is the safe one — it hides finished work rather than inviting a collision — but it is still a line the register wrote and the queue cannot read. **Scope is one line today, measured, not an estimate:** one of 817 parsed claim records. The question is whether the grammar should grow a case, and it is not obvious that it should | T | Measure first and report before writing a parser case. Count every register line that carries a release or merge token but whose parsed id is NOT the id the token sits beside, and separate "the parser picked a DIFFERENT id on the line" from "the parser picked no id at all". Only then decide, because the two have different fixes and only one of them is a grammar change. **Do not widen the id pattern to `\b[A-Z]-\d{3}\b` anywhere on the line** — that is the move that produced T-545: it would make every id mentioned in passing a candidate subject, and the line above mentions three. If a case is added, a fixture must pin a line whose FIRST `item <id>` is a passing mention and whose real subject leads the sentence, and assert the real subject moves bucket while the mentioned one does not; a fixture with one id on the line cannot fail. **Owed to the map lane, not done here:** `T-510` is not in `scripts/exec/source-stage-map.json`, which is repo-owned and held by another lane, so the board run will name it as unmapped until that lane places it |

<!-- T-591 | source line 8703 | expect lane T -->
| T-591 | **A live refusal control carries an alternative that can never match.** `usableScopeSummary` in `src/app/(maestro)/source/preview/workspace/WorkspaceExecutiveShell.tsx` refuses a value whose text matches `/\b(absent\|unknown\|unresolved\|none\|null\|n\/a\|for_cause_only)\b/i`. The `for_cause_only` alternative is **unreachable**: the string tested is `withoutIdentifierTokens(usableText(value))`, and `withoutIdentifierTokens` replaces every `\b[a-z0-9]+(?:_[a-z0-9]+)+\b` run with a space — so any snake_case token is gone before the list is consulted. Confirmed while building T-588's coverage: a fixture carrying `for_cause_only` is refused only by the *separator* test, and a fixture carrying it without a separator shape is not refused at all — the identifier is stripped and the surrounding prose renders as a reviewed purpose. **This is not a cosmetic dead branch.** The two mechanisms have opposite outcomes on the same input class: the word list **refuses the whole value**, while identifier-stripping **keeps what survives**. So a stored `purpose_summary` reading `Managed services scope for_cause_only` renders as `Managed services scope` — a reviewed-purpose characterisation with a governing clause silently removed from it — where the list's evident intent was to refuse it | T | **Decide which of the two behaviours is intended for a snake_case token inside an otherwise-usable purpose, then make the code say only that.** If stripping is intended, delete the `for_cause_only` alternative, because an unreachable alternative in a refusal control is exactly the "gate that cannot fail" shape this programme exists to repair, and leaving it implies a refusal that does not happen. If refusal is intended, the list must be applied **before** identifier-stripping, and the other alternatives must be re-checked under that order — note `absent`, `unknown`, `unresolved`, `none`, `null` and `n/a` are not snake_case and are reachable today, so changing the order changes behaviour only for the snake_case class. Either way, **prove it with a mutation on a real stored shape rather than a contrived one** — the loaders write this field, so a fixture drawn from what they actually produce is available. Do not settle it by deleting the test that revealed it. **Not a regression and not urgent:** the visible outcome today is a stripped identifier, not a leaked one, which is why T-588 filed it rather than repairing it inside an unrelated change |

<!-- U-512 | source line 8968 | expect lane U -->
| U-512 | **U-511 was one instance of a dead fallback that appears 40 times.** `canonicalClientDisplayName` answers with the `DEFAULT_CLIENT_KEY` option's name for any input it cannot resolve, so its declared `\| null` never occurs — which means **every** `canonicalClientDisplayName(...) ?? <fallback>` in the tree is unreachable. There are **40 of them across 36 files**. Most are surfaces the reader is already inside, where the default account is the right answer and the dead `??` is only noise: `?? "AbarVa Client"` appears 6 times on Source pages, and several pass a second candidate name (`?? event.accountName`, `?? activeClient.name`, `?? clientOption.name`) that a reader would reasonably believe is consulted and never is. **At least two are guards, and those are the ones that matter.** `src/components/home/v4/HomeRecordNotServed.tsx:21` — a record-not-served surface, the same shape as the Source access guard U-511 fixed — intends `?? "this client"` and cannot reach it. `src/app/(maestro)/admin/cross-program-signals/page.tsx:34` intends `?? 'Your tenant'` and cannot reach it. U-511 deliberately did not sweep these: which surfaces are guards is a per-surface judgement and a mechanical replacement would change what ~34 non-guard surfaces render for no stated reason | U | **Classify before changing anything, and record the classification.** For each of the 40, say whether the surface is one the reader is already inside (default account is correct; delete the dead `??` or leave it with a note, but do not silently repoint it) or one that refuses, guards or discloses (must not name an account it has not resolved; repoint to `canonicalClientDisplayNameOrNull`, which U-511 added for exactly this). **Prove each guard by rendering with the tenant unresolved**, as U-511 did — a call-site diff is not proof, because the defect was never visible in the call site, only in what it rendered. The two named above are known-true positives available today: a repair that does not change what they render did not work. **Do not fix this by changing `getClientOption`'s default or by narrowing `canonicalClientDisplayName`'s return type** — the first has product-wide blast radius and the second churns roughly 150 callers to no behavioural end. Where a fallback carries a second candidate name rather than a literal, decide whether that candidate was meant to be authoritative; if it was, the call site is wrong in a second way and that is the more interesting half |

<!-- T-705 | source line 10090 | expect lane T -->
| T-705 | **The rung-7 veto is narrower than the negations the register actually writes, so a sentence that DENIES signed-in proof is read as asserting it.** The rule matches `\blive-proven\b` anywhere in a sentence and vetoes only `\bnot\s+(?:live-proven\|signed-in)\b` — the two words adjacent, nothing between. Measured on `ffe95143d` after T-702, over the 38 items that read rung 7: **2 of them take rung 7 from a sentence whose own words negate it.** `T-557` quotes "**This line does not claim deployed or live-proven.**" — `not` is followed by `claim`, so the veto misses, and the item reads `Signed-in proven` from a line written to say the opposite. Item `59` quotes "NOT \`live-proven\` — signed-in", where a single backtick between the two words defeats `\s+`. This is the same shape as the blocked-rule gap filed as T-703, on a different rule, and it is not latent: rung 7 is the queue's `isFinished` test, so both items are excluded from the blocked bucket as finished work | T | Widen the veto to the negation forms the register writes — an intervening word, and markup between `not` and the term — proven the same way the decision rule was: negated and past-tense uses do not match, genuine proof still does. **`T-557` and item `59` are the real known positives and both must move off rung 7**; a fixture that writes `not live-proven` with the two words adjacent cannot fail this, because that form is already vetoed. **Do not widen the rung-7 TEST in the same change** — only its veto — so the movement can be attributed. Publish the rung histogram and the bucket delta, and state the direction: T-702's repair moved 105 items and every one moved DOWN, which is what made it safe |

<!-- C-009 | source line 4024 | expect lane MALFORMED -->
| C-009 | **An advisor answer inside the length budget is still destructured, and three quarters of it is dropped.** `paragraphSplit` in `src/lib/answer/shared-response-shaper.ts` splits on a bare `\n` as well as a blank line, so `compactForChat`'s `maxParagraphs` budget counts **lines, not paragraphs**. A fixture of **861 chars (under the 900 target) in 5 paragraphs (at the budget) but 6 lines** is judged over budget, fully destructured, and rebuilt as a lead plus one bullet. The lead is chosen by keyword match (`vendor|risk|value|…`), which selects the **closing question** and promotes it to the first line. This is `shapeAgentResponseForSurface` → `shapeSharedAdvisorResponse`, which `useAgentStream` calls on every agent turn with no options, so it is every surface, not one | C | `decision needed`. **Recommendation:** count paragraphs as paragraphs — changing the split to `/\n\s*\n/` makes the pinned marker in `intelligence-chat-shape.test.ts` fail, i.e. makes the full answer round-trip, and was verified as a mutation. But `paragraphSplit` is also used to report `length_over_target` and to pick the lead paragraph, so the one-line change moves three behaviours at once and belongs to whoever owns answer rendering. Do not take this as a test-lane edit. The marker is `it.failing`, so it fails the moment the behaviour changes either way |

<!-- T-545 | source line 7984 | expect lane MALFORMED -->
| T-545 | **A claim line is read as a RELEASE because the word appears anywhere on it.** `readClaims()` in `build-execution-queue.mjs` decides the release verdict with `/\bRELEASED\b/.test(line)` over the whole line, and the id with a separate match. The two are never related to each other, so a line that claims one item while *mentioning* that some other item's files were released reads as a release of the item it claims. **Found live, on this item's own claim line**: T-530's 18:02Z claim says another agent "RELEASED it at 17:18Z" about a file, 798 characters away from the `item T-530` reference, and the next queue regeneration listed T-530 under *Explicitly released* while it was actively held. An agent reading that queue would have been invited straight into a collision. Measured over the register: **102 lines parse as a release; on 5 of them the `RELEASED` token is 462–913 characters from the id reference** (`#25` twice, `T-530` twice, `#54` once), while every genuine release has it within ~11 characters. This is the same defect T-457 repaired for *merge* attribution — "any non-negated merge word anywhere on the line" — and the repair shape is already proven there **RESOLVED 2026-09-21 — merged.** PR #8178, GitHub `mergedAt` **2026-09-21T21:24:51Z**, squash `989db2363c54d3e69ab6aff736a1fa96bde95b8f`, confirmed an ancestor of `origin/main`; 34 checks passed / 5 skipping / 0 failed, 35 created, `MERGEABLE CLEAN`. **The premise held and was wrong in one detail, corrected rather than repeated:** it named the five far lines as `#25` twice, `T-530` twice and `#54` once — measured, the fifth is **`#126`**, whose line reads `RELEASED T-005 | ... | item 126`; the `#54` line reads `RELEASED item 54` with the token adjacent and is a GENUINE release this change leaves alone. The verdict now follows the release token NEAREST the id reference, rejected by a preceding negator, by another item id lying between the id and the token, and by a measured reach of 120 characters. **Two rules, because a reach alone cannot catch `RELEASED T-005 | ... | item 126` and an intervening-id rule alone cannot catch a closing sentence that releases a FILE claim 631 characters later.** Measured on ONE frozen register snapshot with both sides generated in the same run, not from T-530's quoted numbers: 817 lines parse as claim records, 108 read as a release before and 103 after, and **all five flips are misattributions flipping toward `held`** — the safe direction, since a false hold hides work while a false release causes the collision. **Exactly one id moves bucket today: `#126`, `released` → `expired-in-flight`.** The other four are no longer the newest line for their id — but they were, and that is shown rather than asserted: replaying the register truncated immediately after the 18:02Z line, the old code puts **T-530 under *Explicitly released* while it was actively held** and the new code puts it under *Expired but WORK IN FLIGHT — do not take*. Suite 53 → 59 cases, red-first 3 of 6 new cases failing before the fix and 0 after; the fixture pins a line that claims one item while mentioning another's release, and two anti-tautology cases stop a fix that simply never releases anything. **Eight mutations, eight caught, after ONE RECORDED ESCAPE that was the suite's fault and not the guard's** — taking the first token instead of the nearest changed nothing, because no case had two release tokens on one line and the selection loop was an unreached branch; case 20f now puts a far token about another lane's fixture before the id and a near one after it. Two further fixtures had the same class of defect in draft (their branch names repeated the item id, so the intervening-id rule rejected the line before the negator or the reach could be reached) and were rewritten to isolate one rule each. CI-side proof the control RUNS: run `35656193975`, step *Run the execution queue behavioral contract*, all four new case titles in the log, `59 passed, 0 failed`. **Residual, stated:** the reach is a measurement of this register, not a proof — the furthest genuine release is 54 and the nearest misattribution 251, an empty band with better than 2x margin either way, but a genuine release written with more than 120 characters between id and token would read as held. **No signed-in acceptance is owed or claimed** — nothing under `src/` imports the changed script. Left behind deliberately and filed as **T-510**: id *parsing* is unchanged, so the `item 126` line still never sees the id it meant to release. | T | Attribute the release verdict to the merge/release token **nearest the id reference**, exactly as `register-time-authority.mjs` now does for merges, and report the residual. A fixture must pin a line that claims one item while mentioning another item's release and assert the claimed item stays **held** — a fixture with only a well-formed release line cannot fail. Record the bucket counts before and after against a baseline you generate yourself in the same run — do not reuse the `0 / 134 / 6 / 110 / 102 / 65` snapshot quoted in T-530's verdict, which the claim lines written since have already moved — and name every id that moves, because this one does move items between buckets. Do not restamp or edit any existing register line to make it parse correctly — the correction pattern is append-only, and one such correction is already appended at 18:12Z |
