#!/usr/bin/env node

console.log(
  [
    "Home V4 preview reconciliation is retired.",
    "The active Home route is /home and is backed by the AI Success Command Center data adapter.",
    "Do not recreate the retired V4 preview route or renderer package.",
  ].join("\n"),
);
