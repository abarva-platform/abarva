{
  "description": "Packet 30 storage-adapter residual allowlist. Runtime storage paths now use the Azure object-storage adapter; only the legacy compatibility helper file remains.",
  "maxFiles": 1,
  "maxImportMatches": 1,
  "files": [
    "src/lib/supabase-server.ts"
  ]
}
